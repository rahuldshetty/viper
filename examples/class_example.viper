class Scone {
  fn topping(first, second) {
    print "scone with " + first + " and " + second;
  }
}

scone = Scone();

print Scone
print scone
print scone.topping

scone.topping("berries", "cream");

